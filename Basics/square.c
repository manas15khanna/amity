//Area of rectangele= side x side
#include <stdio.h>
#include <math.h>
int main()
{
    int a;
    printf("Enter Side of Square: ");
    scanf("%d",&a);
    float d=a*a;
    printf("Area of the Square is %f", d);
    return 0;
}