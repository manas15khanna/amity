//Area of rectangele= length x breadth
#include <stdio.h>
#include <math.h>
int main()
{
    int a;
    int b;
    printf("Enter Length and Breadth of Rectangle: ");
    scanf("%d %d",&a,&b);
    float d=a*b;
    printf("Area of the Rectangle is %f", d);
    return 0;
}
